
import dataServer from './dataServer';
import issueServer from './issueServer';
import robotControlServer from './robotControlServer';

const fields = {
    dataServer,
    issueServer,
    robotControlServer
};

export default fields;