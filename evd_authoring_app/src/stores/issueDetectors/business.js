// import { generateUuid } from "../generateUuid"

export const findCycleTimeIssues = (program) => { // Requires trace timing + delay and machine wait primitives (start computation after move_unplanned in initialize)
    let issues = {};

    return issues;
}

export const findIdleTimeIssues = (program) => { // Use delay and machine wait primitives (delays can be tweaked if application acceptable)
    let issues = {};

    return issues;
}

export const findReturnOnInvestmentIssues = (program) => { // cycle time
    let issues = {};

    return issues;
}