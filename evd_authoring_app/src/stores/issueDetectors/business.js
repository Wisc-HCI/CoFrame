// import { generateUuid } from "../generateUuid"

export const findCycleTimeIssues = (program) => {
    let issues = {};

    return issues;
}

export const findIdleTimeIssues = (program) => {
    let issues = {};

    return issues;
}

export const findReturnOnInvestmentIssues = (program) => {
    let issues = {};

    return issues;
}